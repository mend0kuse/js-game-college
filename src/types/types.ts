export type Step = 'chooseMap' | 'game' | 'stats'

export interface ITrap {
    posX: number
    posY: number
    id: number
}

export interface IMonster {
    posX: number
    posY: number
    id: number
}